import React, { memo} from 'react';
// import { useStyles } from "./styles";

const Merchant = memo(() => {

    return (
        <div>
            Merchant Page      
        </div>
    );
})

export default Merchant;
