import React, { memo} from 'react';
// import { useStyles } from "./styles";

const Home = memo(() => {

    return (
        <div>
            Home Page      
        </div>
    );
})

export default Home;